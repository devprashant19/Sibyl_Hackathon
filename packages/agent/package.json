{
  "name": "@sibyl-agent",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "zod": "^3.23.0",
    "@sibyl-shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}